<?xml version="1.0" encoding="UTF-8"?>
 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="dev.mvc.board_recommend.Board_recommendDAOInter">
  <!-- 게시글 추천 등록 -->
  <insert id="create" parameterType="dev.mvc.board_recommend.Board_recommendVO">
    INSERT INTO board_recommend(board_recommendno, boardno, memberno, rdate)
    VALUES (board_recommend_seq.nextval, #{boardno}, #{memberno}, sysdate)
  </insert>
  
  <!-- 게시글 추천 전체 목록 -->
  <select id="list_all" resultType="dev.mvc.board_recommend.Board_recommendVO">
    SELECT board_recommendno, boardno, memberno, rdate
    FROM board_recommend
    ORDER BY board_recommendno DESC
  </select>

  <!-- 게시글 추천 조회 -->
  <select id="read" resultType="dev.mvc.board_recommend.Board_recommendVO" parameterType="int">
    SELECT board_recommendno, boardno, memberno, rdate
    FROM board_recommend
    WHERE board_recommendno = #{board_recommendno}
  </select>  
     
  <!-- 게시글 추천 삭제 -->
  <delete id="delete" parameterType="int">
    DELETE FROM board_recommend
    WHERE board_recommendno = #{board_recommendno}
  </delete>
  
  <!-- 특정 컨텐츠의 특정 회원 추천 갯수 산출, 하나의 글에 대하여 중복 추천 안됨 -->
  <select id="hartCnt" resultType="int" parameterType='HashMap'>
    SELECT COUNT(*) as cnt
    FROM board_recommend
    WHERE boardno=#{boardno} AND memberno=#{memberno}
  </select>
  
  <!-- boardno, memberno로 조회 -->
  <select id="readByBoardnoMemberno" resultType="dev.mvc.board_recommend.Board_recommendVO" parameterType="HashMap">
    SELECT board_recommendno, boardno, memberno, rdate
    FROM board_recommend
    WHERE boardno=#{boardno} AND memberno=#{memberno}
  </select>  
      
</mapper>

