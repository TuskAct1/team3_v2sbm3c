<?xml version="1.0" encoding="UTF-8"?>
 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="dev.mvc.board.BoardDAOInter">
  <!-- 게시글 등록 -->
  <insert id="create" parameterType="dev.mvc.board.BoardVO">
    INSERT INTO board(boardno, categoryno, memberno, title, content, passwd, word, rdate, visible,
                                file1, file1saved, thumb1, size1)
    VALUES(board_seq.nextval, #{categoryno}, #{memberno}, #{title}, #{content}, #{passwd}, #{word},  sysdate, #{visible},
                #{file1}, #{file1saved}, #{thumb1}, #{size1})
  </insert>

  <!-- 모든 카테고리의 등록된 게시글 목록 -->
  <select id="list_all" resultType="dev.mvc.board.BoardVO">
    SELECT boardno, categoryno, memberno, title, content, passwd, word, rdate, visible, cnt,
               file1, file1saved, thumb1, size1, recom
    FROM board
    ORDER BY boardno DESC
  </select>
  
  <!-- 일반적인 게시글 조회 -->
  <select id="read" resultType="dev.mvc.board.BoardVO" parameterType="int">
    SELECT boardno, categoryno, memberno, title, content, passwd, word, rdate, visible, cnt,
               file1, file1saved, thumb1, size1, recom
    FROM board
    WHERE boardno = #{boardno}
  </select>

  <!-- 게시글 삭제 -->
  <delete id="delete" parameterType="int">
    DELETE FROM board
    WHERE boardno=#{boardno}
  </delete>

  <!-- 카테고리 선택시 관련 글 목록 -->
  <select id="listByCategory" resultType="dev.mvc.board.BoardVO">
    SELECT boardno, categoryno, memberno, title, content, passwd, word, rdate, visible, file1, file1saved, thumb1, size1, cnt, recom
    FROM board
    WHERE categoryno = #{categoryno}
    ORDER BY boardno DESC
  </select>

  <!-- 게시글 수정 -->
  <update id="update" parameterType="dev.mvc.board.BoardVO">
    UPDATE board
    SET title = #{title}, content = #{content}
    WHERE boardno = #{boardno}
  </update>

  <!-- 조회수 증가 -->
  <update id="increaseCnt" parameterType="int">
    UPDATE board
    SET cnt = cnt + 1
    WHERE boardno = #{boardno}
  </update>

  <!-- 게시글 추천 수 증가 -->
  <update id="increaseRecommend" parameterType="int">
    UPDATE board
    SET recom = recom + 1
    WHERE boardno = #{boardno}
  </update>

  <!-- 게시글 추천 수 감소 -->
  <update id="decreaseRecommend" parameterType="int">
    UPDATE contents
    SET recom = recom - 1
    WHERE boardno = #{boardno}
  </update>


</mapper>