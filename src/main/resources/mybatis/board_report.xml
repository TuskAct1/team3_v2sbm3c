<?xml version="1.0" encoding="UTF-8"?>
 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="dev.mvc.board_report.Board_reportDAOInter">
  <!-- 게시글 신고 등록 -->
  <insert id="create" parameterType="dev.mvc.board_report.Board_reportVO">
    INSERT INTO board_report(board_reportno, boardno, memberno, rdate)
    VALUES (board_report_seq.nextval, sysdate, #{boardno}, #{memberno})
  </insert>
  
  <!-- 게시글 신고 전체 목록 -->
  <select id="list_all" resultType="dev.mvc.board_report.Board_reportVO">
    SELECT board_reportno, boardno, memberno, rdate
    FROM board_report
    ORDER BY board_reportno DESC
  </select>

  <!-- 게시글 신고 조회 -->
  <select id="read" resultType="dev.mvc.board_report.Board_reportVO" parameterType="int">
    SELECT board_reportno, boardno, memberno, rdate
    FROM board_report
    WHERE board_reportno = #{board_reportno}
  </select>  
     
  <!-- 게시글 신고 삭제 -->
  <delete id="delete" parameterType="int">
    DELETE FROM board_report
    WHERE board_reportno = #{board_reportno}
  </delete>
  
  <!-- 특정 컨텐츠의 특정 회원 신고 갯수 산출, 하나의 글에 대하여 중복 신고 안됨 -->
  <select id="reportCnt" resultType="int" parameterType='HashMap'>
    SELECT COUNT(*) as cnt
    FROM board_report
    WHERE boardno=#{boardno} AND memberno=#{memberno}
  </select>
  
  <!-- boardno, memberno로 조회 -->
  <select id="readByBoardnoMemberno" resultType="dev.mvc.board_report.Board_reportVO" parameterType="HashMap">
    SELECT board_reportno, boardno, memberno, rdate
    FROM board_report
    WHERE boardno=#{boardno} AND memberno=#{memberno}
  </select>  
      
</mapper>

