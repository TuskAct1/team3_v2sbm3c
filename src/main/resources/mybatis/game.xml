<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="game">

    <!-- 오늘 플레이 횟수 조회 -->
    <select id="checkToday" parameterType="int" resultType="int">
        SELECT COUNT(*)
        FROM game_log
        WHERE memberno    = #{memberno}
        AND TRUNC(log_time) = TRUNC(SYSDATE)
    </select>

    <!-- 플레이 로그 저장 -->
    <insert id="logGame" parameterType="int">
        INSERT INTO game_log (
        game_log_id,
        memberno,
        log_time
        ) VALUES (
        game_log_seq.NEXTVAL,
        #{memberno},
        SYSDATE
        )
    </insert>

</mapper>
