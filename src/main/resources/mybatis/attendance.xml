<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dev.mvc.attendance.AttendanceDAOInter">

  <insert id="create" parameterType="dev.mvc.attendance.AttendanceVO">
    INSERT INTO attendance (
      attendanceno, memberno, total_days, last_check
    ) VALUES (
      attendance_seq.nextval,
      #{memberno},
      #{total_days},
      #{last_check, jdbcType=VARCHAR}
    )
  </insert>
  
  <select id="readByMemberno" resultType="dev.mvc.attendance.AttendanceVO" parameterType="int">
    SELECT * FROM attendance WHERE memberno = #{memberno}
  </select>
  
  <update id="updateForToday" parameterType="int">
    UPDATE attendance
    SET total_days = total_days + 1,
        last_check = TO_CHAR(SYSDATE, 'YYYY-MM-DD')
    WHERE memberno = #{memberno}
  </update>
  
  <!-- ✅ 중복 제거 & 패키지명 명시 -->
  <select id="findByMemberno" parameterType="int" resultType="dev.mvc.attendance.AttendanceVO">
    SELECT * FROM attendance
    WHERE memberno = #{memberno}
  </select>

  <!-- ✅ 파라미터 타입 명확히 지정 -->
  <insert id="insert" parameterType="map">
    INSERT INTO attendance (memberno, last_check)
    VALUES (#{memberno}, #{date})
  </insert>

  <update id="update" parameterType="map">
    UPDATE attendance
    SET last_check = #{date}
    WHERE memberno = #{memberno}
  </update>

</mapper>
