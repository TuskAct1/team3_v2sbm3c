<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="attendance">

  <!-- 오늘 출석 여부 확인 -->
  <select id="checkToday" parameterType="int" resultType="int">
    SELECT COUNT(*)
    FROM attendance
    WHERE memberno      = #{memberno}
    AND TRUNC(last_check) = TRUNC(SYSDATE)
  </select>

  <!-- 멤버 총 출석일수 (attendance.total_days 사용) -->
  <select id="countByMemberno" parameterType="int" resultType="int">
    SELECT total_days
    FROM attendance
    WHERE memberno = #{memberno}
  </select>

  <!-- 연속 출석 일수 반환 -->
  <select id="getTotalDays" parameterType="int" resultType="int">
    SELECT total_days
    FROM attendance
    WHERE memberno = #{memberno}
  </select>

  <insert id="initAttendance" parameterType="int">
    INSERT INTO attendance (
    attendanceno, memberno, total_days, last_check
    ) VALUES (
    attendance_seq.NEXTVAL,
    #{memberno}, 1, SYSDATE
    )
  </insert>

  <update id="markAttendance" parameterType="int">
    UPDATE attendance
    SET total_days = total_days + 1,
    last_check = SYSDATE
    WHERE memberno = #{memberno}
  </update>
</mapper>
